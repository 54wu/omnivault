# 安全说明 Security

感谢你关注 **OmniVault · 万象档案袋** 的安全。这是一个本地的个人上下文加密工具，我们非常重视安全与隐私。

## 安全模型（简版）

- 每个字段用 **AES-256-GCM** 单独加密，密钥由档案密码经 **Argon2id** 派生，再经 **HKDF** 展开为各字段子密钥。
- **零知识（0-knowledge）**：档案密码从不落盘；解锁期间密钥只存在于内存。
- **密钥永不离机**：`secret.key` 与加密数据分离，建议单独存放于安全的外部介质。
- 运行时默认监听 `127.0.0.1`，通过服务令牌/会话鉴权，令牌有作用域并设有效期。

详细说明见 README 的「[加密原理](./README.md#加密原理安全模型)」与「[跨设备同步](./README.md#跨设备同步云同步--密钥分离)」章节。

## 什么在支持范围内

当前 `main` 分支的最新版本处于积极维护。历史版本或未打标签的构建仅供参考，不承诺修复。

## 报告漏洞

**请勿在公开的 Issue、论坛或聊天中发布安全漏洞细节**——这会给仍在升级路径上的用户带来风险。

首选通过 GitHub 的**私有漏洞报告（Private vulnerability reporting）**提交：

> https://github.com/54wu/omnivault/security/advisories

GitHub 会将该报告作为私有 Security Advisory 处理，仅对你与维护者可见，并支持协同修复与发布。

### 报告内容

请尽量提供：

- 受影响的组件与版本（`main` 分支提交哈希 或 Release 标签）
- 漏洞类型（如加密滥用、越权、令牌/密钥泄露、注入等）
- 可复现步骤与环境描述
- 任何缓解手段（如有）

### 我们的承诺

- 对所报漏洞进行**及时评估与响应**，通常会在数天内给出初步反馈。
- 在修复可用或风险可控前，对你的报告严格保密。
- 修复合并并发布后，可在你许可的情况下在致谢中写入你的名字。

## 非漏洞的安全咨询

关于密钥存放、备份、Agent 与令牌的使用方式等，更适合用普通 Issue 或 Discussions 讨论——它们不影响他人安全，也不属于保密的范畴。

## 最后

请始终遵循“最小权限”：给代理/服务只授权的 Scope，妥善保管 `secret.key`，为档案设置足够强度的密码。